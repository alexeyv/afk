# Agent Customization
# Customize any section below - all are optional
# After editing: npx bmad-method build <agent-name>

# Override agent name
agent:
  metadata:
    name: ""

# Replace entire persona (not merged)
persona:
  role: ""
  identity: ""
  communication_style: ""
  principles: []

# Add custom critical actions (appended after standard config loading)
critical_actions:
  - "After completing each task (Step 8), commit with: {type}({story-key}): {task-description}"
  - "After story completion (Step 9), commit before marking status 'review'"
  - "Before any commit, run quality checks: ruff check, ruff format --check, pyright, pytest"
  - "Never push. Human reviews and pushes."
  - "Always use `git mv` for renames/moves, never plain `mv`"
  - "Commit messages use conventional format with outcome footer (outcome: success/failure)"

# Add persistent memories for the agent
memories:
  - "Amend only for typos; new commit for substantive changes"
# Example:
# memories:
#   - "User prefers detailed technical explanations"
#   - "Current project uses React and TypeScript"

# Add custom menu items (appended to base menu)
# Don't include * prefix or help/exit - auto-injected
menu: []
# Example:
# menu:
#   - trigger: my-workflow
#     workflow: "{project-root}/custom/my.yaml"
#     description: My custom workflow

# Add custom prompts (for action="#id" handlers)
prompts: []
# Example:
# prompts:
# - id: my-prompt
#   content: |
#     Prompt instructions here
